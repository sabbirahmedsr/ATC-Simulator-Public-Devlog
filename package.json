{
  "name": "atc-simulator-devlog",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://sabbirahmedsr.github.io/ATC-Simulator-Public-Devlog",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}